import app from "./hono";

console.log("[Backend/Index] Exporting Hono app v1.3.5 - Backend active with all routes");
console.log("[Backend/Index] Routes available: landmarks.discover, landmarks.getAll, tts.generate, tours.save");

export default app;
